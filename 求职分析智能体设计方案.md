# 求职分析智能体设计方案(对齐实现 v0.2)

> 本文档与当前仓库代码逐行对齐,后续修改请先更新本设计,再改代码。

## 0. 项目快照

| 项 | 值 |
|---|---|
| 类型 | FastAPI + LangGraph 求职分析智能体 |
| 默认 LLM | DeepSeek v4 Flash(OpenAI 兼容协议) |
| 可选 LLM | OpenAI(改 `.env` 即可) |
| 主链路 | PDF / DOCX / TXT 简历 + JD 文本/文件 → 匹配报告 + 优化建议 |
| 双通道 | 同步 `POST /api/v1/analyze` + 流式 `POST /api/v1/analyze/stream` |
| 缓存层 | PostgreSQL + SQLAlchemy 2.0 async + JSONB |
| 面试题预测 | 独立页面 + 缓存回填,仅 1 次 LLM 调用 |
| 前端 | `static/index.html` + `app.js` + `app.css`(主分析) + `interview.*`(面试题) |
| 容器 | `Dockerfile` + `.dockerignore`,已通过本地挂载验证可启动 |
| 部署默认端口 | `0.0.0.0:8000` |
| Python | 3.10+ |

## 1. 系统架构

### 1.1 一句话定位

面向**求职者**的轻量级简历 ↔ 岗位匹配分析服务,把 PDF/DOCX 简历 + 岗位 JD 丢进去,几秒钟拿到带分数、可解释、可执行的差距分析与改写建议。

### 1.2 分层架构

```text
┌────────────────────────────────────────────────────────────────────┐
│                          API 层 (FastAPI)                            │
│  路由 · 参数校验 · 文件分块读取 · MIME/大小/互斥校验 · trace_id        │
│  HTTP 异常统一映射 (415/413/422/400/500) · 同步 + 流式双通道            │
│  面试题预测路由 · 缓存管理路由                                         │
├────────────────────────────────────────────────────────────────────┤
│                          Service 层                                  │
│  AnalyzerService.analyze          · 同步入口(ainvoke)                 │
│  AnalyzerService.analyze_stream   · 流式入口(NDJSON 事件流)           │
│  InterviewService.predict         · 面试题预测(缓存读取 + LLM)         │
├────────────────────────────────────────────────────────────────────┤
│                       LangGraph 工作流层                              │
│            ┌─→ parse_resume ─┐                                       │
│  START ────┤                 ├─→ dispatch_matchers ─┬→ skill_gap  ┐  │
│            └─→ parse_job  ──┘                        ├→ experience├──┴→ aggregate → generate_suggestions → END
│                                                       └→ keywords  ┘   ↑
├────────────────────────────────────────────────────────────────────┤
│                            业务组件层                                 │
│  parsers/text_extractor   提取 + 清洗                                 │
│  extractors/*             LLM 结构化抽取 + 流式回调                     │
│  matchers/*               技能(LLM 语义+字面回退) / 经验 / 关键词         │
│  workflow/suggestion_generator   建议生成(LLM + 启发式回退)              │
│  workflow/progress        阶段表 + 线程安全进度回调                       │
├────────────────────────────────────────────────────────────────────┤
│                          缓存层                                       │
│  core/database   SQLAlchemy 2.0 async 引擎 + session 工厂             │
│  core/cache      缓存服务(save / load / list / delete)               │
│  models/cache    AnalysisCache ORM 模型(JSONB)                        │
│  alembic/        数据库迁移                                           │
├────────────────────────────────────────────────────────────────────┤
│                          基础设施层                                   │
│  core/config    provider-aware 配置(DeepSeek/OpenAI) + DATABASE_URL   │
│  core/errors    AppError 体系 → 4xx/5xx 映射                          │
│  core/logging   控制台 + 滚动文件日志                                  │
│  core/metrics   进程内请求计数/最近日志/平均耗时(根路径用)               │
│  static/*       前端(主分析 + 面试题预测,无构建步骤)                     │
└────────────────────────────────────────────────────────────────────┘
```

### 1.3 数据流

```text
HTTP 上传简历 + JD(文本或文件)
   │
   ▼
API 层:分块读文件 + 后缀/MIME/大小校验 + trace_id
   │
   ▼
parsers.text_extractor: PDF/DOCX/TXT → 清洗后的纯文本
   │
   ▼
extractors: 调用 LLM(chat_json + Schema 校验 + 重试 1 次)
   ├─→ ResumeExtractor  → ResumeData
   └─→ JobExtractor     → JobRequirement
   │
   ▼(并行)
matchers:
   ├─ skill_gap(LLM 语义匹配,失败回退字面)
   ├─ experience(规则 + 年限)
   └─ keywords(规则)
   │
   ▼
aggregate_report 汇总 → MatchReport
   │
   ▼
generate_suggestions(LLM,失败回退启发式)
   │
   ▼
AnalyzerService: 组装 {meta, match_report, suggestions}
   │
   ├─→ API 层: JSON 或 NDJSON 流返回
   │
   └─→ 缓存层: PostgreSQL 写入(异步,失败不影响主流程)
           │
           ▼
       面试题预测页面: 从缓存读取 → 1 次 LLM → 8-12 道题
```

### 1.4 当前工作流图(LangGraph)

```text
                  ┌─→ parse_resume ─┐
  START ──────────┤                 ├─→ dispatch_matchers (fan-in)
                  └─→ parse_job  ──┘                │
                                                     │ add_conditional_edges(Send fan-out)
                                                     ▼
                              skill_gap ┐
                              experience├──→ aggregate_report ─→ generate_suggestions → END
                              keywords  ┘
```

- **并行抽取**:`parse_resume` / `parse_job` 都从 `START` 扇出,互不依赖,节省 1 次 LLM 串行耗时
- **fan-in 汇聚点**:`dispatch_matchers` 是个空节点,避免两条扇入边触发 LangGraph 的 `InvalidUpdateError`
- **并行匹配**:`Send` 模式把状态同时推给三个匹配节点,谁先算完谁先返回,总耗时取最长者
- **fan-in 汇总**:`aggregate_report` 等待三个匹配节点全部完成
- **同步 ainvoke / 流式 astream**:两套入口复用同一张图

### 1.5 端到端接口

| 方法 | 路径 | 用途 |
|---|---|---|
| `GET` | `/` | 运维监控着陆页(深色玻璃拟态,BOOT 日志 + 端点表 + 最近请求) |
| `GET` | `/app` | 用户前端应用(上传简历 + 粘贴 JD + 流式进度可视化) |
| `GET` | `/interview` | 面试题预测页面(需 `?trace_id=xxx`) |
| `GET` | `/docs` | Swagger UI |
| `GET` | `/api/v1/health` | 存活探针 |
| `GET` | `/api/v1/info` | 服务元信息(provider / model / 端点列表) |
| `POST` | `/api/v1/analyze` | **同步分析**,返回 JSON |
| `POST` | `/api/v1/analyze/stream` | **流式分析**,返回 NDJSON,含 token 级进度 |
| `POST` | `/api/v1/interview/predict` | **面试题预测**(非流式) |
| `POST` | `/api/v1/interview/predict/stream` | **面试题预测**(NDJSON 流式) |
| `GET` | `/api/v1/cache` | 列出缓存的分析记录(分页) |
| `GET` | `/api/v1/cache/{trace_id}` | 获取单次分析完整缓存数据 |
| `DELETE` | `/api/v1/cache/{trace_id}` | 删除单次缓存 |
| `GET` | `/static/*` | 前端静态资源 |

## 2. 关键设计

### 2.1 统一解析链路

简历和 JD 都走 `extract_text → clean_text → structured_extract`:

- [app/parsers/text_extractor.py](file:///c:/Users/10408/Desktop/job/app/parsers/text_extractor.py) 暴露 `extract_text()` 与 `extract_text_from_bytes()`,统一处理 PDF/DOCX/TXT
- 失败归类为 `ParseError`(422) / `UnsupportedFileTypeError`(415)
- 抽取层只负责"文本 → Pydantic 模型",不混入业务

### 2.2 LLM 客户端统一封装

[app/extractors/llm_client.py](file:///c:/Users/10408/Desktop/job/app/extractors/llm_client.py) 是唯一和 LangChain / OpenAI 兼容协议打交道的入口:

- `chat_json(system, user, schema, ...)`:用 Pydantic schema 约束输出,失败自动重试一次
- `chat_text(...)`:自由文本输出
- **流式回调**:有 `progress_callback` 时走 `model.stream`,支持 token 级推送
- **回调来源优先级**:`显式参数 > ContextVar > 线程安全共享变量 > None`,确保同步/流式/工作线程三种路径都能拿到
- **provider 透明**:`.env` 中 `LLM_PROVIDER=deepseek/openai` 切换 0 业务代码改动

### 2.3 工作流的"双层并行"

| 阶段 | 并行方式 | 收益 |
|---|---|---|
| 抽取 | `add_edge(START, "parse_resume")` + `add_edge(START, "parse_job")` | 省 1 次 LLM 串行耗时 |
| 匹配 | `Send` fan-out 到 3 个节点 | 总耗时取最长者,3× → 1× |
| 建议生成 | 串行 | LLM 1 次,无法并行 |

### 2.4 进度推送(流式核心)

[app/workflow/progress.py](file:///c:/Users/10408/Desktop/job/app/workflow/progress.py) 定义了**前后端共用的单一事实源** `STAGES`,共 8 个阶段,每个阶段有固定 percent 区间:

```text
upload 0-5  | parse_resume 5-20 | parse_job 20-35
skill_gap 35-65 | experience 65-78 | keywords 78-88
aggregate 88-92 | suggestions 92-99
```

#### 推送链路

```text
LLMClient._invoke_stream(...)
   │ chunk / first_token / streaming(每 80 字符)
   ▼
progress_callback(phase, {chars})      [在 LangGraph 工作线程中]
   │
   ▼ loop.call_soon_threadsafe
asyncio.Queue.put_nowait(...)
   │
   ▼ 主生成器消费
yield NDJSON: {type: "progress", percent, message, chars}
```

#### 阶段追踪

- `analyzer` 在调用工作流前通过 `set_progress_callback` / `set_progress_callback_shared` 同时设置 `ContextVar` 和模块级共享变量
- LangGraph 在线程池中跑节点,ContextVar 不能跨线程,所以用 `set_current_stage_shared(stage)` 让 LLMClient 在工作线程里也能读到当前阶段
- 每个节点用 `_with_stage_tracking` 装饰,执行前设置当前阶段,执行后记录耗时

#### NDJSON 事件流

```json
{"type":"meta","trace_id":"...","stages":[...]}
{"type":"stage_start","key":"parse_resume","percent":10.0}
{"type":"progress","percent":12.5,"message":"已处理 80 字符","chars":80}
{"type":"progress","percent":15.0,"message":"已处理 500 字符","chars":500}
{"type":"stage_end","key":"parse_resume"}
...
{"type":"done","data":{...},"duration_ms":12345}
```

#### 异常透传

- 节点抛 `AppError` → 推送 `{"type":"error","stage":...,"code":...,"message":...}`
- 节点抛其他异常 → 推送 `{"type":"error","stage":...,"code":"workflow_error","message":...}`
- langgraph 0.2.x 的 `astream` 不发 `__end__`,因此用累积的 `initial` 作为 `final_state`

### 2.5 匹配设计

| 匹配器 | 实现 | LLM 依赖 | 失败回退 |
|---|---|---|---|
| 技能 | `_semantic_skill_gap` + `_literal_skill_gap` | 1 次 LLM | 切字面匹配 |
| 经验 | 规则 + 年限计算 | 无 | — |
| 关键词 | 规则(子串归一化) | 无 | — |

#### 技能语义匹配关键点

- **单次 LLM 调用**:把简历原文 + 硬/软技能列表一并塞进 prompt,一次返回所有判断
- **等价物参考**:`PgVector ≡ Milvus / Qdrant`、`FastAPI(asyncio) ≈ aiohttp / Sanic`、`SQLAlchemy ≈ SQLModel` 等
- **括号示例不构成硬要求**:`LLM (OpenAI / DeepSeek) 等至少一种` 只需要命中 LangGraph/LangChain 就算 matched
- **三级容错查找**:精确 → 归一化(`re.sub(r"[\s/(),]+", "", s)`) → 短文本包含
- **Pydantic 强约束**:`_SkillEntry` + `_SkillGapSchema` 严格约束 LLM 输出

#### 技能匹配入口 API

```python
# 默认:LLM 语义匹配,失败回退字面
gap = analyze_skill_gap(resume, job, llm_client=LLMClient())

# 强制字面(性能对比 / 离线测试)
gap = analyze_skill_gap(resume, job, force_literal=True)

# 不传 client(等同字面)
gap = analyze_skill_gap(resume, job)
```

### 2.6 降级策略

| 失败点 | 降级方案 |
|---|---|
| 文档解析失败 | 返回明确错误码,API 422 |
| LLM JSON 解析失败 | 重试 1 次;仍失败则 `ExtractError` 抛出 |
| 技能语义匹配失败 | 自动切到字面匹配(同 API) |
| 建议生成失败 | 切到启发式建议:基于 missing/keywords/经验年限 给出保守建议 |
| LLM key 缺失 | 启动时仍允许启动,首次请求 502 |
| 数据库不可用 | 缓存写入失败静默降级,主分析不受影响;面试题预测返回 503 |

### 2.7 上传安全

| 项 | 实现 |
|---|---|
| 文件后缀白名单 | `.pdf / .docx / .txt`(可配置 `UPLOAD_ALLOWED_EXTS`) |
| MIME 白名单 | `application/pdf / application/vnd.openxmlformats-officedocument.wordprocessingml.document / text/plain` |
| 大小限制 | 默认 20MB,通过分块读取累计,提前抛错避免内存炸 |
| 参数互斥 | `job_description` 与 `job_file` 不能同时传,也不能都为空 |
| 错误码 | `UnsupportedFileTypeError` 415、`FileTooLargeError` 413、`ParseError` 422 |

### 2.8 分析缓存层

#### 选型:PostgreSQL + JSONB

本项目的"缓存"本质是**持久化 + 关联查询**,而非传统临时加速层:

- **持久化需求**:分析结果需跨会话保留,Redis 的 TTL 机制与此矛盾
- **关联查询**:面试题预测需从 match_report 提取 skill_gap,从 resume_data 提取项目经历
- **JSONB 原生支持**:存储深层嵌套 Pydantic 对象,支持 GIN 索引和 JSON 路径查询
- **扩展性**:未来 RAG 知识库可直接使用 pgvector 扩展
- **async 契合**:SQLAlchemy 2.0 async + asyncpg 与 FastAPI 生态完全一致

> Redis 定位:未来如需 L1 加速层(短时间重复请求秒回),可加 Redis,与 PostgreSQL 不冲突。

#### ORM 模型

```python
class AnalysisCache(Base):
    __tablename__ = "analysis_cache"
    trace_id: Mapped[str]          # PK, UUID
    created_at: Mapped[datetime]
    resume_name: Mapped[str|None]  # 列表查询用
    job_title: Mapped[str|None]    # 列表查询用
    overall_score: Mapped[float]   # 列表查询用
    resume_data: Mapped[dict]      # JSONB,完整 Pydantic 序列化
    job_requirement: Mapped[dict]  # JSONB
    match_report: Mapped[dict]     # JSONB
    suggestions: Mapped[dict]      # JSONB
    meta: Mapped[dict]             # JSONB
```

**JSONB vs 独立列**:ResumeData、MatchReport 字段多且嵌套深,拆为独立列维护成本极高。JSONB 存完整对象,读取后 `model_validate()` 还原为 Pydantic 模型,兼顾灵活性和类型安全。

#### 写入时机

`analyzer.py` 的 `analyze()` 和 `analyze_stream()` 完成后,通过 `_persist_cache()` 异步写入:

```python
async def _persist_cache():
    async with async_session_factory() as session:
        await save_analysis(session, trace_id, ...)
```

写入失败仅记录 WARNING,不抛异常,不影响主分析结果返回。

### 2.9 面试题预测

#### 架构:独立页面 + 缓存回填

面试题预测**不修改现有工作流**,而是作为独立模块:

```text
主分析完成 → 缓存写入 PostgreSQL
                    │
                    ▼
/interview?trace_id=xxx → 从缓存读取 → 1 次 LLM → 8-12 道题
```

#### 题型分布

| 类型 | 数量 | 难度 | 来源 |
|---|---|---|---|
| technical | 4-6 | medium/hard | skill_gap.missing / partial |
| behavioral | 2-3 | medium | experience.notes |
| project_deep_dive | 2-3 | hard | resume_data.projects + skill_gap |
| situational | 0-1 | medium | job_requirement + experience |

#### 数据模型

```python
class InterviewQuestion(BaseModel):
    type: Literal["technical","behavioral","project_deep_dive","situational"]
    difficulty: Literal["easy","medium","hard"]
    question: str
    related_skill: Optional[str]
    suggested_answer_direction: str   # 3-5 个关键要点
    follow_up: Optional[str]

class InterviewPredictionOutput(BaseModel):
    questions: list[InterviewQuestion] = Field(min_length=8, max_length=12)
    summary: str
```

#### 降级策略

- 缓存不存在 → 404,前端显示"请先完成分析"
- LLM 生成失败 → 重试 1 次,仍失败返回 502
- 数据库不可用 → 主分析不受影响,面试题功能不可用

## 3. 数据模型(对齐 `app/models/`)

| 文件 | 主要类 |
|---|---|
| `resume.py` | `ContactInfo / Education / WorkExperience / ProjectExperience / Certification / ResumeData` |
| `job_requirement.py` | `Priority / RequirementCategory / Requirement / JobRequirement` |
| `suggestion.py` | `SuggestionType / SuggestionPriority / ResumeSection / ResumeSuggestion / SkillMatchItem / SkillGapAnalysis / ExperienceMatch / KeywordMatch / MatchReport` |
| `response.py` | `ApiResponse[T] / AnalyzeRequestMeta / AnalysisResult` |
| `interview.py` | `QuestionType / Difficulty / InterviewQuestion / InterviewPredictionOutput` |
| `cache.py` | `AnalysisCache`(SQLAlchemy ORM,JSONB 字段) |

要点:

- `contact` 是强类型模型,不再是裸 `dict`
- `Requirement` 带 `category / priority / evidence` 三个元信息
- `ResumeSuggestion` 同时带 `type / priority / section / current / suggestion / example / reason / related_jd_requirement`
- `MatchReport` 是最终报告类型,聚合 `SkillGapAnalysis / ExperienceMatch / KeywordMatch / hard_requirements_gaps / overall_score`
- API 响应统一用 `ApiResponse[T] = {success, code, message, data, trace_id}`

## 4. 目录结构(实际)

```text
c:\Users\10408\Desktop\job\
├── app/
│   ├── main.py                          # FastAPI 入口 + lifespan + 静态资源
│   ├── api/
│   │   ├── routes.py                    # /analyze, /analyze/stream, /health, /info
│   │   └── routes_interview.py          # 面试题预测 + 缓存管理路由
│   ├── core/
│   │   ├── config.py                    # provider 感知配置 + DATABASE_URL
│   │   ├── database.py                  # SQLAlchemy 2.0 async 引擎 + session 工厂
│   │   ├── cache.py                     # 缓存服务(save / load / list / delete)
│   │   ├── errors.py                    # AppError 体系
│   │   ├── logging.py                   # 控制台 + 滚动文件
│   │   └── metrics.py                   # 进程内指标
│   ├── parsers/
│   │   └── text_extractor.py            # PDF/DOCX/TXT → 文本
│   ├── extractors/
│   │   ├── llm_client.py                # 统一 LLM 客户端(流式 + 回调 + ContextVar/共享变量双轨)
│   │   ├── resume_extractor.py          # 简历结构化
│   │   └── job_extractor.py             # JD 结构化
│   ├── matchers/
│   │   ├── skill_matcher.py             # LLM 语义 + 字面回退
│   │   ├── experience_matcher.py        # 规则 + 年限
│   │   └── keyword_matcher.py           # 规则
│   ├── workflow/
│   │   ├── graph.py                     # LangGraph 拓扑(Send fan-out/fan-in + 并行抽取)
│   │   ├── state.py                     # AgentState + _keep_last reducer
│   │   ├── nodes.py                     # 节点实现
│   │   ├── progress.py                  # 阶段表 + 线程安全进度回调 + compute_streaming_percent
│   │   └── suggestion_generator.py      # 建议生成 + 启发式回退
│   ├── models/
│   │   ├── resume.py                    # ResumeData / ContactInfo / WorkExperience …
│   │   ├── job_requirement.py           # JobRequirement / Requirement …
│   │   ├── suggestion.py               # MatchReport / SkillGapAnalysis / ResumeSuggestion …
│   │   ├── response.py                 # ApiResponse[T] / AnalysisResult
│   │   ├── interview.py                # InterviewQuestion / InterviewPredictionOutput
│   │   └── cache.py                    # AnalysisCache ORM 模型(JSONB)
│   └── services/
│       ├── analyzer.py                  # AnalyzerService(同步 ainvoke + 流式 Queue 模式)
│       └── interview_service.py         # InterviewService(缓存读取 + LLM 面试题生成)
├── alembic/                             # 数据库迁移
│   └── versions/0001_add_analysis_cache.py
├── static/
│   ├── index.html                       # GET /app 入口
│   ├── app.js                           # 上传 + 流式消费
│   ├── app.css                          # 暗色玻璃拟态
│   ├── interview.html                   # 面试题预测页面
│   ├── interview.js                     # 面试题页面交互
│   └── interview.css                    # 面试题页面样式
├── uploads/                             # 临时文件目录
├── logs/                                # 日志目录
├── .env / .env.example
├── .dockerignore
├── .gitignore
├── Dockerfile
├── requirements.txt
├── README.md
├── DEVELOPMENT.md
└── 求职分析智能体设计方案.md             # 本文档
```

## 5. 启动与配置

### 5.1 本地启动

```bash
# 激活环境(项目惯例:D:\miniconda\envs\fastapi)
D:\miniconda\envs\fastapi\Scripts\activate.bat
cd C:\Users\10408\Desktop\job
pip install -r requirements.txt
copy .env.example .env
# 编辑 .env,填入 DEEPSEEK_API_KEY(默认走 DeepSeek v4 Flash)

# 启动 PostgreSQL(首次需先初始化)
D:\miniconda\envs\fastapi\Library\bin\initdb.exe -D "D:\pgdata" -U postgres -E UTF8 --locale=C
D:\miniconda\envs\fastapi\Library\bin\postgres.exe -D "D:/pgdata" -p 5432
# 另开终端创建用户和数据库:
D:\miniconda\envs\fastapi\Library\bin\psql.exe -U postgres -h 127.0.0.1 -p 5432 \
  -c "CREATE USER job WITH PASSWORD 'job';" \
  -c "CREATE DATABASE job_analyzer OWNER job;" \
  -c "GRANT ALL PRIVILEGES ON DATABASE job_analyzer TO job;"
D:\miniconda\envs\fastapi\Scripts\alembic.exe upgrade head

# 启动服务
uvicorn app.main:app --reload --port 8000
# 或
D:\miniconda\envs\fastapi\python.exe -m uvicorn app.main:app --reload --port 8000
```

### 5.2 Docker 启动

```bash
docker build -t job-analyzer:test .
docker run --rm -p 8000:8000 --env-file .env job-analyzer:test
```

> 当前环境 Docker Hub 拉取基础镜像受限,推荐用本地已有 Python 镜像挂载源码调试。

### 5.3 .env 关键项

| 变量 | 说明 | 默认 |
|---|---|---|
| `LLM_PROVIDER` | `deepseek` / `openai` | `deepseek` |
| `LLM_MODEL` | 模型名 | `deepseek-v4-flash` |
| `DEEPSEEK_API_KEY` | DeepSeek key(优先于 OPENAI_API_KEY) | — |
| `OPENAI_API_KEY` | OpenAI key(回退) | — |
| `LLM_BASE_URL` | 自定义 base_url | provider 默认 |
| `LLM_TIMEOUT` | LLM 超时秒 | `60` |
| `DATABASE_URL` | PostgreSQL 连接串 | `postgresql+asyncpg://job:job@localhost:5432/job_analyzer` |
| `UPLOAD_DIR` | 临时文件目录 | `./uploads` |
| `UPLOAD_MAX_MB` | 文件大小上限 | `20` |
| `UPLOAD_ALLOWED_EXTS` | 后缀白名单 | `.pdf,.docx,.txt` |
| `APP_DEBUG` | debug 模式 | `false` |
| `LOG_LEVEL` | 日志级别 | `INFO` |

### 5.4 切换 OpenAI

```dotenv
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o-mini
OPENAI_API_KEY=sk-your-openai-key
LLM_BASE_URL=
```

业务代码不感知 provider,所有 LLM 调用统一经过 `app/extractors/llm_client.py`。

## 6. 风险与对策

| 风险 | 表现 | 对策 |
|---|---|---|
| 真实密钥泄露 | `.env` 误传 git | `.gitignore` 兜底;DeepSeek/OpenAI 控制台可轮换 |
| PDF 难解析 | 文本缺失 / 顺序乱 | 优先支持文本型 PDF;扫描版走 OCR(已留扩展位) |
| LLM 输出不稳定 | JSON 非法 / 字段缺失 | Schema 校验 + 重试 1 次 + 启发式降级 |
| 长任务前端黑盒 | 用户以为卡死 | 流式 token 级进度 + 阶段百分比 |
| 大文件上传炸内存 | OOM | 分块读取 + 累计判断 + 提前 413 |
| 双通道接口漂移 | 同步和流式行为不一致 | `analyze` 与 `analyze_stream` 共用同一个 `AgentState` 初始结构 |
| LangGraph 版本变更 | `astream` 不发 `__end__` | 用累积 `initial` 作 `final_state` |
| 工作线程拿不到 ContextVar | 进度回调丢失 | 双轨:`ContextVar + 线程安全共享变量`,优先取有效值 |
| PostgreSQL 连接异常 | 缓存写入/读取失败 | 连接池重试 + 主分析流程不依赖缓存(缓存失败不影响分析) |
| 数据库迁移失败 | 表结构不一致 | Alembic 版本管理 + 迁移前备份 |

## 7. 实施路线图

### Phase 1 · MVP(已完成)
- [x] FastAPI + LangGraph 主链路
- [x] PDF/DOCX/TXT 统一解析
- [x] LLM 结构化抽取(简历 + JD)
- [x] 三层匹配(技能 / 经验 / 关键词)
- [x] 同步分析接口 `/api/v1/analyze`
- [x] 建议生成 + 启发式回退
- [x] 同步 + 流式双通道
- [x] 抽取并行:parse_resume ∥ parse_job
- [x] 匹配并行:Send fan-out
- [x] 技能匹配:字面 → LLM 语义(可回退)
- [x] 流式 token 级进度
- [x] 前端工作台 / 着陆页
- [x] 容器化与可观测指标

### Phase 2 · v0.2(已完成)
- [x] 分析缓存层(PostgreSQL + SQLAlchemy 2.0 async + JSONB)
- [x] 面试题预测(独立页面 + 缓存回填,8-12 道题)
- [x] 缓存管理 API(list / get / delete)
- [x] 数据库迁移(Alembic)
- [x] 前端面试题预测页面(interview.html / js / css)

### Phase 3 · 质量增强
- [ ] 扫描版 PDF 的 OCR 兜底(rapidocr-onnxruntime)
- [ ] 建议多轮反思(自评 + 改写)
- [ ] 节点级重试与降级
- [ ] 单元 / 接口 / 端到端测试
- [ ] 页面状态恢复(sessionStorage + 缓存回填)

### Phase 4 · 产品化
- [ ] 多对多简历 ↔ JD 批量匹配
- [ ] 历史记录 + 报告导出
- [ ] 简历评分面板
- [ ] 简历库 + 向量检索(pgvector)
- [ ] 前端拆为独立 Vite + React 项目

---

**最后更新**:2026-06-10 · 维护者:cxzrdxy
